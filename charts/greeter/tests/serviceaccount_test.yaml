suite: serviceaccount
templates:
  - serviceaccount.yaml
tests:
  - it: creates a ServiceAccount by default
    asserts:
      - isKind:
          of: ServiceAccount
  - it: has the correct name
    asserts:
      - equal:
          path: metadata.name
          value: greeter
